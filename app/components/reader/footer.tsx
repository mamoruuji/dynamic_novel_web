import { useTheme } from "@emotion/react";
import React from "react";

const footer = () => {
  const theme = useTheme();
  return (
    <>
    <footer>
    </footer>
    </>
  );
};

export default footer;
