export type Dynamic = {
  dynamicId: number;
  title: string;
  overview: string;
  userId: string;
  published: boolean;
  createdAt: string;
  updatedAt: string;
}

export type page = {
  pageId: number;
  title: string;
  order: number;
  chapterId: number;
}
