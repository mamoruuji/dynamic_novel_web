export type DynamicType = {
  dynamicId: number
  title: string
  overview: string
  userId: string
  published: boolean
  createdAt: string
  updatedAt: string
}

export type PageType = {
  pageId: number
  title: string
  order: number
  chapterId: number
}
